export const metadata = { title: "Requests — AlumniConnect" };

// Placeholder: request/connection flow lands in build-order step 7.
export default function RequestsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Mentorship requests</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Request management is coming in a later sprint.
      </p>
    </div>
  );
}
